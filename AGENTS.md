# LangChain / LangGraph / LangSmith 多 Agent 协作开发规范

## 1. 项目目标与适用范围

本仓库用于通过“工业设备故障研判与工单闭环 Agent”学习并实践 LangChain、LangGraph 与 LangSmith。

最终业务链路是：

`用户问题 -> 结构化解析 -> 设备/测点/报警/工单/知识检索 -> 证据检查 -> 故障研判 -> 风险判断 -> 人工审批 -> 模拟业务动作 -> 带证据的诊断报告`

本文件对仓库根目录及所有子目录生效。所有主 Agent、子 Agent 和审查 Agent 在开始工作前都必须读取并遵守本文件。更深目录若存在自己的 `AGENTS.md` 或 `AGENTS.override.md`，以更具体且不违反上级安全边界的规则为准。

项目必须同时满足两个目标：

1. 产出可运行、可测试、可观测的 Agent 工程。
2. 让学习者能够解释每个模块的责任、真实数据流、状态变化、失败路径和安全边界。

不得为了展示框架而堆叠无业务价值的 Agent、工具、向量库或外部服务。RAG 是 Agent 可调用的证据工具，不是整个应用。

## 2. 总体原则

### 2.1 主控原则

- 主 Agent 是唯一总负责人，负责理解需求、拆分任务、冻结契约、分配 Owner、控制写入顺序、集成结果和最终验收。
- 子 Agent 只处理明确分配的任务，不得自行扩大业务范围、修改公共契约或增加外部副作用。
- 单个子 Agent 的 `PASS` 不代表整体完成；最终状态只能由主 Agent依据全部测试、审查和验收证据判定。
- 主 Agent 必须等待当前阶段全部子 Agent 返回，或者明确记录其失败、超时、不可用及受影响范围后，才能进入下一阶段。
- 缺失结果会影响关键架构、安全判断或验收结论时，必须停止实施并标记 `BLOCKED`；否则最多以 `PARTIAL` 继续并披露缺口。

### 2.2 证据原则

- 结论优先级：当前源码与运行结果 > 当前配置 > 官方文档 > 项目文档 > 历史说明或推测。
- 涉及版本易变化的 LangChain、LangGraph、LangSmith API 时，优先核对当前官方文档和已锁定依赖，不照搬旧博客代码。
- 不得把导入成功、函数返回 `True`、HTTP 200、存在 Trace、测试发现 0 项或静态配置文件存在当作完整业务成功。
- 所有“已完成”“已通过”“可恢复”“已持久化”“已发送到 LangSmith”等结论必须附可复查命令、测试结果、Trace/Experiment 证据或明确的代码路径。
- 未执行的运行、构建、外部服务连接和付费模型调用必须标为“未验证”，不得推断成功。

### 2.3 学习优先原则

- 每个里程碑都必须解释“为什么这样设计”，而不只给出代码。
- 优先使用当前 LangChain v1 风格 API，例如 `langchain.agents.create_agent`；引入 `langchain-classic` 或旧式 Agent API 必须说明不可替代的原因。
- 外层业务流程应尽量确定性，动态推理限制在明确的 Agent 节点内。
- 先用可控的模拟工具完成契约和测试，再接入真实数据库、工单系统或设备平台。
- 中文文档和关键中文注释应解释责任、状态转换、数据来源、副作用与失败边界；不要给显而易见的语句逐行加注释。

## 3. 项目阶段和范围边界

按下列顺序推进，除非用户明确调整优先级：

1. **LangChain 基础 Agent**：模型适配、工具、结构化输出、Middleware、基础 Trace。
2. **LangGraph 状态编排**：State、Node、Edge、条件路由、并行查询、Reducer、错误分支。
3. **持久化与人工审批**：Checkpoint、`thread_id`、Interrupt、恢复、幂等副作用。
4. **RAG 与记忆**：设备手册/SOP 检索、来源引用、短期记忆、受控长期记忆。
5. **LangSmith 评测**：Dataset、Evaluator、Experiment、最终回答/单步骤/轨迹评测。
6. **工程化**：FastAPI、SSE、权限、限流、重试、Docker、CI 和部署文档。

每一阶段都必须能独立运行和演示。后续阶段不得掩盖前一阶段未关闭的 P0/P1 问题。

计划中的目录边界如下；实际创建前仍须由主 Agent 在任务计划中冻结：

| 路径 | 默认责任 |
| --- | --- |
| `app/models/`、`app/config/` | 模型提供商适配、配置与依赖注入 |
| `app/schemas/` | 请求、工具参数、Graph State、诊断结果契约 |
| `app/tools/` | 设备、测点、报警、工单等工具及模拟实现 |
| `app/agents/` | 有边界的 LangChain Agent 与 Middleware |
| `app/graphs/` | LangGraph 节点、路由、持久化与审批流程 |
| `app/retrieval/` | 文档摄取、检索、证据与引用处理 |
| `app/observability/` | LangSmith Trace、标签、脱敏与辅助逻辑 |
| `app/api/` | FastAPI、SSE、线程与审批接口 |
| `evaluations/` | LangSmith 数据集、评测器、实验入口与固定样例 |
| `tests/` | 独立冻结的单元、契约、集成与安全测试 |
| `docs/` | 中文学习笔记、架构、数据流、运行和复盘材料 |

## 4. Agent 角色

角色按任务需要启用，不为占满并发槽位而创建无价值子任务。

### 4.1 主 Agent：`main_controller`

- 读取用户目标、本文件、当前仓库状态和已有改动。
- 建立当前分支、HEAD、`git status --short` 基线；若不是 Git 仓库，明确说明且不得自行初始化。
- 拆分可独立验证的任务，给每个子 Agent 完整任务契约。
- 汇总调查结论，冻结接口、状态、文件 Owner、测试期望和非目标。
- 控制共享目录内的写入顺序，复核每次修改和差异范围。
- 汇总审查意见、组织最多两轮修复与复测，并输出最终状态。

### 4.2 只读调查角色

- `official_docs_researcher`：核对当前官方 API、版本迁移、限制和推荐模式，返回直接来源；不得修改代码。
- `code_flow_mapper`：追踪真实入口、配置、调用链、Graph 状态、工具数据流和副作用；不得把未调用代码当成有效业务流。
- `test_risk_analyst`：冻结验收场景，检查测试入口、异步/并发/持久化风险和不可测边界。
- `security_analyst`：检查权限、Prompt Injection、敏感数据、工具越权、外部副作用和依赖风险。

### 4.3 实施角色

- `langchain_worker`：模型适配、工具契约、结构化输出、Agent 与 Middleware。
- `langgraph_worker`：Graph State、节点、路由、Checkpoint、Interrupt 和恢复。
- `retrieval_worker`：文档摄取、检索、证据元数据和引用。
- `langsmith_worker`：Tracing、Dataset、Evaluator 和 Experiment。
- `api_worker`：FastAPI、SSE、线程及审批接口。
- `test_worker`：在测试侧独立实现冻结的契约和回归场景。
- `learning_docs_worker`：将已验证行为整理成可复述的中文学习材料。

实施角色只是默认能力分工。每次任务仍必须指定精确文件 Owner；角色名称不自动获得整个模块的写权限。

### 4.4 独立审查角色

- `correctness_reviewer`：检查状态更新、路由、边界、幂等、恢复语义和行为回归。
- `test_reviewer`：运行测试，检查断言是否独立、覆盖是否有效、是否存在 0 测试或假阳性。
- `security_reviewer`：检查数据泄露、注入、权限、危险工具和依赖风险。
- `eval_observability_reviewer`：检查 Trace 是否完整、评测数据是否有效、指标是否能发现真实失败。
- `learning_reviewer`：检查中文材料是否与当前代码一致，是否区分已实现、计划中和未验证能力。

审查 Agent 第一轮只能只读，不得边审边修。

## 5. 子 Agent 任务契约

主 Agent 给每个子 Agent 的任务必须包含：

1. **目标**：本子任务要回答或交付什么。
2. **允许范围**：可读取的目录和可修改的精确文件。
3. **禁止范围**：不得修改的文件、公共接口和外部系统。
4. **写入权限**：只读或可写；未明确即视为只读。
5. **上下文**：当前 Git/文件基线、用户已有改动、上游契约和已知风险。
6. **必须返回的证据**：文件与行号、命令输出、测试结果、官方链接或 Trace/Experiment 标识。
7. **完成标准**：可判定的功能、测试和文档要求。
8. **操作授权**：是否允许联网、安装依赖、调用付费模型、写入 LangSmith、访问仓库外路径、修改外部系统或执行破坏性操作；未明确授权一律禁止。
9. **失败收口**：超时、工具不可用、外部服务失败时必须返回什么。

子 Agent 必须按以下格式返回：

```text
STATUS: PASS | PARTIAL | BLOCKED
SUMMARY: 简要结论
EVIDENCE: 文件:行号、命令与关键结果、必要的官方来源
FILES_READ: 实际读取范围
FILES_CHANGED: 实际修改文件；只读任务写 none
TESTS: 执行命令、结果与未运行原因
RISKS: 剩余风险和未验证范围
HANDOFF: 主 Agent 下一步需要做什么
```

状态定义：

- `PASS`：全部完成标准满足且有可复查证据；没有执行必要检查不能判定为 `PASS`。
- `PARTIAL`：已完成可安全交付部分，但存在不影响当前关键验收的缺口，必须逐项披露。
- `BLOCKED`：缺少源码、权限、依赖、外部状态或关键证据，导致关键验收或高风险判断无法完成。

## 6. 标准协作流程

### 6.1 阶段 A：基线与只读调查

主 Agent 首先执行：

- 读取本文件及更具体目录规则。
- 检查目录、Git 状态、现有文件、运行环境和可用测试入口。
- 只报告密钥、Token、连接串、Cookie 等敏感配置的位置和是否配置，严禁回显真实值。
- 非平凡代码、公共契约、安全、持久化或外部副作用变更，根据任务选择 2～4 个互相独立的只读调查 Agent 并行工作。
- 简单、低风险、无运行行为变化的文档或格式任务，主 Agent 可说明理由后减少调查角色；修改本协作规范本身时仍至少安排一个独立只读审查 Agent。

调查阶段所有子 Agent 都禁止修改文件、安装依赖、写入外部服务或运行具有业务副作用的工具。

### 6.2 阶段 B：主 Agent 冻结实施契约

进入实施前，主 Agent 必须汇总并明确：

- 当前需求、验收标准、非目标和未解决歧义。
- 真实技术栈、运行入口、测试入口、调用链和数据流。
- 计划修改文件以及一一对应的写入 Owner。
- 模型、工具、Graph State、API、SSE、审批、Trace 和 Evaluation 契约中受影响的字段与语义。
- 测试侧独立冻结的输入、输出、状态码、事件序列和关键路径。
- 外部副作用、成本、敏感数据和回滚方案。

任一缺失信息会改变架构或影响高风险判断时，停止实施并标记 `BLOCKED`，不得凭空假设。

### 6.3 阶段 C：受控实施

- 当前 Agent 共享同一工作目录时，同一时间只允许一个 Agent 写文件。
- 每个文件在一个任务周期内只能有一个写入 Owner。
- 多个 Worker 可能接触同一文件、锁文件、数据库、迁移、生成目录或公共测试资源时必须串行。
- 只有主 Agent明确建立互相隔离的 Git worktree/分支且确认资源不共享时，才允许并行写入。
- Worker 发现需要修改范围外文件、公共契约、持久化结构或新增外部服务时必须停止并交回主 Agent 重新规划。
- 禁止顺手重构、覆盖用户改动、批量格式化无关文件、修改真实凭据或生产配置。
- 未经用户明确要求，不提交、不推送、不创建 PR。
- 所有代码修改后都要检查当前差异；不得把其他 Agent 或用户的修改误报为自己的成果。

### 6.4 阶段 D：独立审查

非平凡代码、公共契约、安全、持久化或外部副作用变更完成后至少安排：

1. 正确性审查。
2. 测试审查。
3. 安全审查。

涉及 LangSmith 或评测时增加评测/可观测性审查；涉及学习文档时增加文档一致性审查。

简单、低风险、无运行行为变化的文档或格式任务，主 Agent 可说明理由后按风险减少审查角色，但必须执行适用的结构、链接、格式或一致性检查；修改本协作规范本身时至少保留一个独立只读审查 Agent。

- 第一轮审查必须相互独立，不提前透露其他审查结论，也不暗示期望结果为通过。
- “只读审查”是指不得修改源码、配置、测试断言和交付文档；允许执行已授权测试，并仅在临时目录或已忽略路径产生可清理的缓存、日志、临时数据库和测试报告。
- 每条问题必须包含严重度、文件与行号、复现路径、实际影响和最小修复建议。
- 无法复现的推测只能标记为未验证风险，不得伪装成确定缺陷。
- `P0`：可能造成严重安全、数据损坏或不可恢复影响，必须立即停止。
- `P1`：影响关键验收、核心正确性或存在可利用风险，完成前必须关闭。
- `P2`：有证据的非阻断问题，可以在说明影响后延期。

### 6.5 阶段 E：修复、复测与结束

- 主 Agent 对每条审查意见记录 `接受`、`拒绝` 或 `延期`，并提供证据和理由。
- `P0/P1` 不得延期；必须修复，或用可复现证据证明问题不成立。
- 最多执行两轮“审查 -> 修复 -> 复测”。第二轮只验证问题关闭和是否引入回归。
- 两轮后仍存在 P0/P1 或关键验收失败时，整体状态必须为 `BLOCKED`。
- 仅剩已披露且不影响关键验收的 P2 或非关键未验证项时，整体最多为 `PARTIAL`。

## 7. LangChain / LangGraph / LangSmith 专项规则

### 7.1 模型与 LangChain Agent

- 模型提供商必须通过配置或适配层注入，业务模块不得散落读取环境变量。
- `.env.example` 只能出现占位值，任何日志、测试快照、Trace 和文档都不得包含真实密钥。
- 单元测试默认使用 fake/stub 模型和确定性工具；真实模型调用必须是显式的 live test，能单独开启和关闭。
- 不得将失败的真实模型调用静默替换为 stub 后仍宣称 live test 通过。
- Tool 必须有清晰名称、单一责任、类型化参数、可验证返回值和明确错误语义。
- 模型自然语言不得直接作为数据库、命令行或危险 API 的未校验参数。
- 关键结果必须使用结构化输出并做 Schema 校验；解析失败必须进入显式错误或重试分支。
- Middleware 的重试、模型降级、PII 处理、调用次数限制必须有测试，不得只写配置。

### 7.2 LangGraph 状态与执行

- Graph State 保存原始、可复用、可序列化的数据，不保存只适用于某个 Prompt 的拼接文本。
- 节点应单一职责，优先返回状态增量，不隐式修改共享全局对象。
- 路由条件必须可测试；关键安全路由不得只依赖模型自由文本。
- 并行节点合并必须定义 Reducer 或确定的冲突策略，并测试执行顺序变化。
- Checkpoint、Interrupt、恢复和时间旅行能力必须通过真实中断/恢复测试证明。
- 使用 `thread_id` 隔离会话，测试不同线程之间无状态泄漏。
- Interrupt 前发生的副作用必须幂等，或移动到批准后的独立节点；恢复不得重复创建工单。
- 工具超时、临时错误、用户可修复错误和不可恢复错误必须进入不同处理路径。
- 不允许无限 Agent 循环；必须设置模型调用、工具调用或图步数上限。

### 7.3 RAG、记忆与不可信内容

- 文档、网页、工具返回值和历史对话都属于不可信输入，不得覆盖系统规则或授权策略。
- 检索结果必须携带文档 ID、章节、版本或时间等来源元数据。
- 回答需区分“文档证据”“实时数据”“模型推断”；证据不足时必须追问或拒答。
- 不使用未经评测校准的通用相似度阈值；阈值和检索策略必须用本项目正负样例验证。
- 短期会话状态与跨会话长期记忆必须分开设计、分开授权、分开清理。
- 长期记忆写入需要明确规则，不得把用户临时输入、敏感信息或 Prompt Injection 永久保存。

### 7.4 LangSmith 可观测性与评测

- 每次可观测运行应能关联 request ID、thread ID、Graph/Agent 版本和环境标签，但不得把敏感值作为标签或元数据上传。
- Trace 用于观察执行路径，不自动证明回答正确或副作用成功。
- 评测至少覆盖：最终回答、单步骤工具选择/参数、完整轨迹、结构化输出和安全规则。
- Dataset 的期望值必须在评测侧独立维护，不得从生产实现动态生成预期答案或预期字段。
- 第一批正式评测集目标为 40～60 条，覆盖正常、多工具、证据不足、人工审批、工具失败、Prompt Injection 与越权场景。
- 使用 LLM-as-judge 时必须同时保留确定性规则；不能让同一模型无约束地既生成答案又作为唯一裁判。
- Experiment 必须记录模型、Prompt、工具和 Graph 版本元数据，比较结果时同时报告质量、延迟、成本和失败类型。
- 在线评测和生产监控属于后续阶段；未部署时不得声称已完成在线评测。

### 7.5 高风险动作与安全边界

- 停机、创建/修改工单、发送通知、写数据库、执行 SQL/Shell、调用外部生产 API 均视为有副作用工具。
- 开发阶段默认使用模拟实现；接入真实系统必须取得用户明确授权并单独冻结权限、审计和回滚契约。
- 停机、删除、对外发送和生产写入必须经过 Human-in-the-loop；批准、修改、拒绝三种结果都要测试。
- 仅靠 Prompt 声明“不要执行危险动作”不构成安全控制；必须在代码、Middleware 或 Graph 路由中强制执行。
- 外部写操作必须有幂等键、审计记录、超时与明确的部分失败处理。

## 8. 测试与验收矩阵

代码或运行行为变更只运行相关测试不够时，还必须运行项目约定的全量测试。预期存在测试却发现 0 项测试时不得判为通过。仅文档、治理文件或无可执行行为的变更，可用适用的格式、链接、结构、一致性检查和独立审查替代自动化测试，但必须明确记录“无适用运行测试”及替代验证结果。

| 层级 | 最低覆盖要求 |
| --- | --- |
| 单元测试 | Schema、工具参数、路由函数、Reducer、错误分类、脱敏 |
| Agent 测试 | 正确选工具、错误参数、无关问题、最大调用次数、结构化输出 |
| Graph 测试 | 正常路径、证据不足、并行合并、工具失败、Interrupt/Resume、线程隔离 |
| API 测试 | 状态码、请求契约、SSE 事件序列、取消、超时、错误不泄密 |
| 安全测试 | Prompt Injection、工具越权、危险动作未审批、敏感信息进入日志/Trace |
| 评测 | 最终回答、单步骤、轨迹、拒答、引用、延迟和成本 |

测试规则：

- 测试期望不得导入生产代码中的字段列表、路由表或阈值常量作为唯一断言来源。
- 外部模型、LangSmith、数据库和网络测试必须有离线替代测试，并清楚区分 `unit`、`integration`、`live`。
- 并发、网络、时间、流式、Checkpoint 和异步相关测试最终验收前至少连续运行三次。
- 任意一次失败后单独重跑成功仍视为不稳定；不得用无限重试、扩大超时或放宽断言掩盖问题。
- 测试因 SDK、依赖、密钥或外部服务不可用而无法运行时，记录真实命令、错误和未验证范围。关键验收依赖该测试时为 `BLOCKED`，否则最多为 `PARTIAL`。
- 生成的缓存、日志、本地数据库、向量库、测试快照和 Trace 导出必须清理或加入忽略规则，不得污染交付差异。

项目最终阶段的目标指标是：

- 关键结构化输出有效率 100%。
- 高风险动作人工审批覆盖率 100%。
- 固定评测集工具选择正确率不低于 90%。
- 证据不足场景追问或拒答正确率不低于 90%。
- 固定样例关键轨迹稳定率不低于 85%。

这些指标只能基于版本固定、样例可审查的评测集报告；样例不足或未运行时不得宣称达标。

## 9. 文档与学习交付

每个阶段应在 `docs/` 下形成对应中文材料，至少包括：

1. 本阶段要解决的问题与核心概念。
2. 文件到责任的映射。
3. 入口到模型、工具、状态、持久化和输出的真实数据流。
4. 成功路径、失败路径、安全边界和未实现能力。
5. 实际执行命令、测试结果和 LangSmith 观察点。
6. 至少一个失败案例及定位过程。
7. 可由学习者独立完成的练习和验收答案。

文档必须根据当前代码和当前验证结果更新。旧计划、注释、静态配置和未运行示例不能写成当前事实。

## 10. 最终交付门禁

主 Agent 只有在以下条件全部满足时才能报告整体 `PASS`：

- 当前任务验收标准全部满足。
- 代码或运行行为变更的相关测试和项目要求的全量测试真实通过，且不是 0 项测试；仅文档、治理文件或无可执行行为的变更已记录无适用运行测试，并完成适用的替代验证。
- 不存在未关闭的 P0/P1；所有接受的审查问题有修复和回归证据。
- 涉及公共契约或运行行为时，契约测试使用独立基线，并覆盖成功、普通失败、边界、异常与安全路径；纯文档或治理变更记录该项不适用。
- 涉及异步、并发、网络、流式或持久化时，重复测试满足稳定性要求。
- Git 仓库中最终差异无越界修改；非 Git 目录明确说明此检查不可用。
- 没有泄露密钥、个人信息、生产数据或把生成物误加入交付。
- 最终报告列出修改文件、行为变化、测试命令与真实结果、审查意见处理、未验证范围和剩余风险。

最终报告不得使用“应该可以”“理论上已完成”代替验证。存在不影响关键验收的缺口时标记 `PARTIAL`；关键能力无法证明时标记 `BLOCKED`。
